// NewCRPage has been replaced by CRCreationDialog.
// Tests for CR creation are in:
//   - src/components/cr/CRCreationDialog.test.tsx
//   - src/pages/CRListPage.test.tsx

import { describe, it } from "vitest";

describe("NewCRPage (removed)", () => {
  it("has been replaced by CRCreationDialog — see CRCreationDialog.test.tsx", () => {
    // No-op: the page is gone; this file is retained only to satisfy the test runner.
  });
});
